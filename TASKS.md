# loggair — backlog

Open work for this project. Cross-cutting / multi-project initiatives live in the
workspace root `TASKS.md`. Completed items are not archived here — git history is the record.

- [ ] **Loggair Evolution:** Add JSON structured logging and experiment context injection. @medium @observability
- [ ] **Experiment context injection:** automatically inject `epoch` / `step` / `experiment_id` into every log record. @feature
